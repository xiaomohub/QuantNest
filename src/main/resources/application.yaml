spring:
  application:
    name: QuantNest
  datasource:
    url: jdbc:mysql://localhost:3306/trading_os?useUnicode=true&characterEncoding=utf8
    username: root
    password: yourpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

tushare:
  token: "你的tushare token"
  api-url: "https://api.tushare.pro"